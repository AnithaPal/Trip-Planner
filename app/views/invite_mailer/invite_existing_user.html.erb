<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>
      You are invited to participate in this trip <%= @trip.name %>.
      Tripit will help you to discuss about this trip, build an itinerary, and share group expenses.
      Click <%=link_to "here", trip_url(@trip) %>. to view the trip details! Before clicking this link please sign in the application.
    </p>
    <div class="panel panel-default panel-info">
      <div class="panel-heading text-center">
         Are you in?
      </div>
      <div class="panel-body">
        <div class="btn-group">
          <button> <%= link_to "Yes", accept_invite_url(@invite) %> </button>
          <button> <%= link_to "No!", decline_invite_url(@invite) %> </button>
          <button> <%= link_to "More details!!!", trip_url(@trip) %> </button>
       </div>
      </div>
    </div>

    <p> Happy Planning and Tripping!!! </p>

    <p>
      Thanks,<br/>
      <%= @invite.sender.name %>
    </p>
  </body>
</html>
