<div class="container-full">
  <div class="row">
    <div class="col-lg-12 text-center v-center">
      <h1 class="main-heading"> Built For Trippers </h1>
      <p class="lead"> Plan a trip with your friends </p>
      <p>
        We make your trip easy by providing a platform to plan and coordinate with your friends before and after a trip.
      </p>
      <br><br>
        <div class="input-group">
          <%= form_for User.new, url: user_registration_path(), class: "col-lg-12" do |f| %>
            <%= devise_error_messages! %>
            <% if @trip_id %>
              <%= hidden_field_tag :invite_token, params[:invite_token] %>
            <% end %>
            <div class="form-group custom-form">
              <%= f.label :name %>
              <%= f.text_field :name, autofocus: true, class: 'form-control', placeholder: "Enter name" %>
            </div>
            <div class="form-group custom-form">
              <%= f.label :email %>
              <%= f.email_field :email, class: 'form-control', placeholder: "Enter email" %>
            </div>
            <div class="form-group custom-form">
              <%= f.label :password %>
              <%= f.password_field :password, class: 'form-control', placeholder: "Enter password" %>
            </div>
            <div class="form-group custom-form">
              <%= f.label :password_confirmation %>
              <%= f.password_field :password_confirmation, class: 'form-control', placeholder: "Enter password confirmation" %>
            </div>
            <br>
            <div class="form-group custom-form">
              <%= f.submit "Sign up", class: 'btn btn-success' %>
            </div>
            <div class="form-group">
              <%= render "devise/shared/links" %>
            </div>
          <% end %>
    </div>
  </div>
  </div>
  </div>
